name: verify retained backup publication
on:
  push:
    branches: [fix/583-verify]
  workflow_dispatch:
permissions:
  contents: read
jobs:
  native:
    strategy:
      fail-fast: false
      matrix:
        include:
          - runner: ubuntu-latest
            host: linux
            arch: x86_64
          - runner: windows-latest
            host: windows
            arch: x86_64
          - runner: macos-15-intel
            host: darwin
            arch: x86_64
          - runner: macos-latest
            host: darwin
            arch: aarch64
    runs-on: ${{ matrix.runner }}
    timeout-minutes: 50
    steps:
      - uses: actions/checkout@v6
      - uses: ./.github/actions/seed-mach
      - name: Build exact audited compiler serially
        shell: bash
        run: python3 .github/scripts/std-583-backup-bootstrap.py ${{ matrix.host }} ${{ matrix.arch }}
      - name: Verify effects and ownership
        shell: bash
        run: python3 .github/scripts/std-583-backup.py ${{ matrix.host }} ${{ matrix.arch }}
      - uses: actions/upload-artifact@v7
        if: always()
        with:
          name: retained-backup-${{ matrix.host }}-${{ matrix.arch }}
          path: std-583-evidence/
