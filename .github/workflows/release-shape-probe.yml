name: release-shape probe

# temporary (#699): calls ci.yml exactly as release.yml does, to prove a
# workflow_call with heavy=all runs the heavy tier. removed before merge.

on:
  pull_request:

permissions:
  contents: read

jobs:
  native-matrix:
    uses: ./.github/workflows/ci.yml
    with:
      heavy: all
