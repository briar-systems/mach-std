name: verify transaction lifecycle migration
on:
  push:
    branches: [fix/583-verify]
permissions:
  contents: read
jobs:
  native:
    strategy:
      fail-fast: false
      matrix:
        include:
          - runner: ubuntu-latest
            host: linux
            arch: x86_64
          - runner: windows-latest
            host: windows
            arch: x86_64
          - runner: macos-latest
            host: darwin
            arch: aarch64
          - runner: macos-15-intel
            host: darwin
            arch: x86_64
    runs-on: ${{ matrix.runner }}
    timeout-minutes: 40
    steps:
      - uses: actions/checkout@v6
      - uses: ./.github/actions/seed-mach
      - name: Retain released Darwin image and build audited compiler
        if: runner.os == 'macOS'
        run: python3 .github/scripts/std-583-darwin.py ${{ matrix.arch }}
      - name: Retain released seed behavior and build audited compiler
        if: runner.os != 'macOS'
        shell: bash
        run: python3 .github/scripts/std-583-audited.py ${{ matrix.host }} ${{ matrix.arch }}
      - name: Verify transaction lifecycle
        shell: bash
        run: python3 .github/scripts/std-583-lifecycle.py ${{ matrix.host }} ${{ matrix.arch }}
      - name: Prove lifecycle guards fail at runtime
        shell: bash
        run: python3 .github/scripts/std-583-mutations.py ${{ matrix.host }} ${{ matrix.arch }}
      - name: Verify exact standalone native wrapper correction
        if: always() && env.MACH_583_COMPILER != ''
        shell: bash
        run: python3 .github/scripts/std-615-wrapper.py ${{ matrix.host }} ${{ matrix.arch }}
      - uses: actions/upload-artifact@v7
        if: always()
        with:
          name: std-583-${{ matrix.host }}-${{ matrix.arch }}
          path: std-583-evidence/
