name: setup migration mach
description: build the v5 migration compiler from the published seed through the audited chain

outputs:
  compiler:
    description: absolute path to the verified compiler
    value: ${{ steps.bootstrap.outputs.compiler }}

runs:
  using: composite
  steps:
    - name: Install published bootstrap seed
      id: seed
      uses: ./.github/actions/seed-mach
      with:
        tag: v4.26.5
    - name: Build and verify the migration compiler
      id: bootstrap
      shell: bash
      run: python .github/actions/setup-mach/bootstrap.py
    - name: Retain bootstrap provenance
      if: always()
      uses: actions/upload-artifact@v7
      with:
        name: compiler-bootstrap-${{ github.job }}-${{ runner.os }}-${{ runner.arch }}
        path: .mach-toolchain/evidence/
        if-no-files-found: error
