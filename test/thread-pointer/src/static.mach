# the static image: std owns the thread pointer on every thread

use std.runtime;
use thread_pointer.cells;

#[symbol("main")]
fun main(argc: i64, argv: **u8) i64 {
    ret cells.tokens(4, 0, nil);
}
