[project]
id = "drvprobe"
version = "0.0.0"
src = "src"
out = "out/{target.name}/{profile.name}"

[target.linux-x86_64]
isa = "x86_64"
os  = "linux"
abi = "sysv64"

[profile.debug]
opt = 0
debug = true
simd = "scalarize"

[artifact.drvprobe]
kind = "bin"
entry = "main.mach"
out = "bin/drvprobe"
targets = ["*"]
link = []
need = []

[dep.std]
path = "dep/std"
