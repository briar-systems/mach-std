# the fault aggregate with the native adapters and the dispatcher's runtime
# `mach test test/fault --lib tests` runs them, and the fault artifact stays runtime-free

use fault.lib.libfault;
use fault.native;
use fault.test_runtime;
